# 需求拆解与验收条件

## 1. 输入与范围

- **任务**：拆解需求并生成验收条件。
- **已提供业务需求**：未提供。
- **已提供依赖**：空对象 `{}`。
- **本次范围**：将任务本身规范化为可测试要求，并记录阻碍具体业务需求拆解的信息缺口。
- **范围限制**：不得在缺少原始业务需求时虚构用户角色、业务流程、规则、接口、数据或质量指标。

## 2. 需求目标

系统应把原始业务需求转换为明确、原子化、可追踪且可验证的需求项，并为每个可实施需求项生成对应验收条件；信息不足时，系统应显式输出待澄清项，而不是补造业务事实。

## 3. 功能需求与验收条件

### FR-001 接收原始需求

系统应接收一条非空的原始业务需求文本作为分析输入。

**验收条件**

- **AC-FR001-01 有效输入**
  - Given 原始需求去除首尾空白后至少包含 1 个字符
  - When 用户提交分析任务
  - Then 系统接受输入并产生分析结果或明确的处理中状态
- **AC-FR001-02 空输入**
  - Given 原始需求为空、缺失或仅含空白字符
  - When 用户提交分析任务
  - Then 系统明确指出“缺少原始业务需求”，生成补充需求的待澄清项，且不生成具体业务功能或规则

### FR-002 拆解原子需求

系统应将输入拆分为单一职责、可独立验证的需求项。

**验收条件**

- **AC-FR002-01 唯一标识**：每个需求项均具有非空且在本次分析结果内唯一的标识符。
- **AC-FR002-02 原子性**：每个需求项仅包含一个可独立交付的主要系统行为、业务规则或质量约束；若输入包含多个独立行为，则输出多个需求项。
- **AC-FR002-03 强制性表达**：正式需求使用“应”表达强制要求，不使用“尽量”“适当”“友好”“高效”等无量化标准的词作为验收依据。
- **AC-FR002-04 防止臆造**：输出中的每项确定性业务事实均能追踪到原始输入或已确认约束；无法追踪的信息不得写成确定需求。
- **AC-FR002-05 模糊信息处理**：原文存在模糊或冲突内容时，系统保留原始含义并创建待澄清项，不自行设定阈值或业务规则。

### FR-003 生成验收条件

系统应为每个可实施需求项生成至少一条可执行或可人工验证的验收条件。

**验收条件**

- **AC-FR003-01 最低数量**：每个可实施需求项关联的验收条件数量不少于 1。
- **AC-FR003-02 测试结构**：每条验收条件包含前置条件、动作或触发事件、可观察的预期结果，或包含语义等价的 Given/When/Then 表达。
- **AC-FR003-03 可判定结果**：预期结果描述可观察的输出、状态变化或错误行为，不得仅使用“成功”“正常”“良好”等词作为唯一判据。
- **AC-FR003-04 场景覆盖**：在原始需求提供足够规则时，验收条件覆盖正常路径、关键异常路径和边界值；不存在相应场景时明确标记“不适用”，信息不足时创建待澄清项。
- **AC-FR003-05 可追踪性**：每条验收条件均引用或归属于唯一的需求标识符，且被引用的需求真实存在。

### FR-004 识别依赖

系统应识别并记录需求涉及的外部系统、接口、数据、权限、基础设施和前置功能依赖。

**验收条件**

- **AC-FR004-01 无已知依赖**
  - Given 调用方提供的依赖为空
  - And 原始需求未明确提及其他依赖
  - When 系统完成分析
  - Then 依赖状态显示“无已知依赖”，且系统不凭实现惯例推断外部依赖
- **AC-FR004-02 显式依赖**：原始输入明确提及的依赖被记录，并可追踪到对应原文或调用方提供的数据。
- **AC-FR004-03 未确认依赖**：文本提及但细节不足的依赖被标记为“待确认”，并产生说明其影响的待澄清项。

### FR-005 管理信息缺口

系统应把影响需求范围、实现或验收判定的缺失及歧义信息集中记录为待澄清项。

**验收条件**

- **AC-FR005-01 内容完整**：每个待澄清项包含唯一标识、具体问题和影响说明。
- **AC-FR005-02 集中呈现**：所有待澄清项可在统一章节或统一输出字段中检索。
- **AC-FR005-03 事实隔离**：待确认内容不作为已确认业务事实写入正式需求。
- **AC-FR005-04 无具体业务输入**：未提供具体业务需求时，至少询问业务目标、目标角色、使用场景、功能范围、业务规则及非功能约束。

### FR-006 输出完整结果

系统应输出需求目标、需求项、验收条件、依赖状态和待澄清项。

**验收条件**

- **AC-FR006-01 必需章节**：输出包含需求目标、需求项、验收条件、依赖状态和待澄清项；无内容的章节显示“无”“无已知依赖”或“不适用”，不得静默省略。
- **AC-FR006-02 必需字段**：每个需求项至少包含需求标识、需求陈述和关联验收条件。
- **AC-FR006-03 完整关联**：不存在无验收条件的可实施需求项，也不存在无法定位所属需求的验收条件。

## 4. 当前输入的分析结果

当前输入只有任务名称“拆解需求并生成验收条件”，未包含待分析的具体业务需求。因此：

1. 本文已完成对任务级能力的需求规范化；
2. 当前不能确定具体业务角色、流程、规则、异常场景、边界值或非功能指标；
3. 不生成任何登录、支付、报表等示例业务功能作为正式需求；
4. 输入依赖为空且未识别出其他依赖，当前状态为 **无已知依赖**。

## 5. 待澄清项

### Q-001 原始业务需求

- **问题**：需要拆解的具体业务需求、用户故事或产品说明是什么？
- **影响**：缺少该信息时，无法形成具体业务功能需求及对应验收场景。

### Q-002 目标角色与场景

- **问题**：目标用户或外部系统角色是谁，在什么业务场景下使用该能力？
- **影响**：无法确定触发条件、权限边界和正常业务流程。

### Q-003 功能范围

- **问题**：本次需求包含哪些功能，明确不包含哪些功能？
- **影响**：无法确定拆解边界，也无法防止范围遗漏或扩张。

### Q-004 业务规则与异常

- **问题**：适用的业务规则、输入输出、异常处理和边界值是什么？
- **影响**：无法编写可判定的正常、异常及边界验收条件。

### Q-005 非功能要求

- **问题**：是否存在性能、安全、兼容性、可用性或合规指标？若存在，具体阈值和统计口径是什么？
- **影响**：无法形成可量化的非功能验收标准。

### Q-006 外部依赖

- **问题**：是否存在外部系统、接口、数据、权限、基础设施、前置功能或交付时间依赖？
- **影响**：当前只能记录“无已知依赖”，不能据此断言绝对不存在依赖。

## 6. 追踪矩阵

| 目标 | 对应需求 | 对应验收条件 |
|---|---|---|
| 接收分析输入 | FR-001 | AC-FR001-01～02 |
| 原子化需求拆解 | FR-002 | AC-FR002-01～05 |
| 生成可测试验收条件 | FR-003 | AC-FR003-01～05 |
| 记录依赖 | FR-004 | AC-FR004-01～03 |
| 处理缺失与歧义 | FR-005 | AC-FR005-01～04 |
| 输出完整结果 | FR-006 | AC-FR006-01～03 |

## 7. 交付验收

- [x] 已形成书面化、结构化需求。
- [x] 每条任务级需求均具有唯一标识和可测试验收条件。
- [x] 已明确记录“无已知依赖”。
- [x] 已列出缺失的业务信息及其影响。
- [x] 未虚构具体业务规则。

**交付状态：`requirements_written = 通过`。**
